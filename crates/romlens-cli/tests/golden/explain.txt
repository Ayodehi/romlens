$00:8053  STA $4200

  NMITIMEN = $81: NMI on, joypad auto-read on

  Turns interrupts on and off: the NMI at the start of vertical blank, the
  timer IRQ, and the automatic reading of the controllers each frame,
  which takes about three lines at the start of vertical blank. It is zero
  after power-on and reset, so every game sets it.

  Bits  Field      Value  Meaning
  7     NMI        1      NMI on
  4–5   Timer IRQ  0      no timer IRQ
  0     Joypad     1      joypad auto-read on

  NMITIMEN = $81: NMI on, joypad auto-read on
$00:8014  STA $4300

  DMAP0 = $01: 2 registers, alternating (VRAM); BBAD0 = $18…

  $4300 DMAP0
  How this DMA channel copies: which way, how the source address moves
  (up, down, or fixed to fill with one byte), and which pattern of B-bus
  registers it writes. For HDMA the pattern also sets how many bytes each
  line gets.

  Bits  Field          Value  Meaning
  7     Direction      0      from memory to the B bus
  6     HDMA indirect  0      HDMA direct table
  3–4   Source step    0      source address counts up
  0–2   Pattern        1      2 registers, alternating (VRAM)

  $4301 BBAD0
  Which B-bus register ($21xx) this channel writes: $18 is VRAM, $22 the
  palette, $04 sprite memory, $80 work RAM. Some CPU revisions fail to
  start a transfer with $00 here.

  Bits  Field     Value  Meaning
  0–7   Register  $18    to $2118 VMDATAL: VRAM

  DMAP0 = $01: 2 registers, alternating (VRAM)
  BBAD0 = $18: to $2118 VMDATAL: VRAM

▸ DMA transfer  ($00:8014–$00:8035, 6 instructions)
  Channel 0 copies $0800 bytes from $00:9000 to VRAM word $6000.

  The CPU copying a byte at a time is slow. DMA moves data between memory
  and the PPU at about 2.7 MB a second while the CPU waits, which is the
  only practical way to fill VRAM, the palette or sprite memory in the
  short vertical blank.
$00:804E  STA $2100

  INIDISP ← $7E:0DAE
  The value is loaded from $7E:0DAE; its bits are listed without values.

  Turns the picture on or off and sets its brightness. While forced blank
  is on the screen is black and VRAM, CGRAM and OAM can be written at any
  time, which is why games set it before loading graphics. Turning forced
  blank off is best done in vertical blank; mid-frame it glitches the
  sprites on that line and the next. Many games write $8F rather than $80,
  so the brightness does not have to change when the screen comes back on.

  Bits  Field       
  7     Forced blank
  0–3   Brightness
$00:8000: nothing to explain
routines:          4
hardware stores:   14
value known:       13 (93%)
source known:      1 (7%)
indexed, unknown:  0 (0%)
idiom wait:            1
idiom dma:             1
idiom multiply:        1
idiom clear-memory:    1
idiom apu-handshake:   1
idiom decimal:         1
idiom shared-entry:    1
