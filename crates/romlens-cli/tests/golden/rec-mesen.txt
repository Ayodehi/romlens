wrote <tmp>/r.romrec: 12 frames from Mesen, 1 DMA transfers
this Mesen does not export 65 fields, among them cpu.a, cpu.x, cpu.y; their bits are zero
Mesen does not export $212A, $212B, $2130, so they read as zero until the game writes them
Format:       1.2
Producer:     Mesen recorder stream 3
ROM SHA-256:  a2462f1d74f993d0b3e10ae9425cd9b48253ac82c99913f07ef704d9c85dc6ee
              matches romlens-test-lorom.sfc
Frames:       12 (1 keyframe, every 60 frames)
Storage:      6115 bytes, zstd, WRAM in keyframes only
              keyframes 613 bytes each on average, deltas 371
Regions:      cpu 16, ppu 256, io 128, wram 131072, vram 65536, cgram 512, oam 544, timing 16
Layers:       write log, register writes by line
wrote <tmp>/r.romrec: 5 frames from Mesen, 1 DMA transfers
and the sound side: 22 events, 10 of them DSP writes
this Mesen does not export 65 fields, among them cpu.a, cpu.x, cpu.y; their bits are zero
Mesen does not export $212A, $212B, $2130, so they read as zero until the game writes them
Format:       1.2
Producer:     Mesen recorder stream 3
ROM SHA-256:  a2462f1d74f993d0b3e10ae9425cd9b48253ac82c99913f07ef704d9c85dc6ee
Frames:       5 (1 keyframe, every 60 frames)
Storage:      4572 bytes, zstd, WRAM in keyframes only
              keyframes 925 bytes each on average, deltas 568
Regions:      cpu 16, ppu 256, io 128, wram 131072, vram 65536, cgram 512, oam 544, timing 16, aram 65536, dsp 128, spc 32
Layers:       write log, register writes by line, sound events
wrote <tmp>/r.romrec: 12 frames from Mesen, 1 DMA transfers
this Mesen does not export 65 fields, among them cpu.a, cpu.x, cpu.y; their bits are zero
Mesen does not export $212A, $212B, $2130, so they read as zero until the game writes them
00000  00 00 00 00 00 00 00 00 00 00 00 00 0B 80 00 00
wrote <tmp>/r.romrec: 11 frames from Mesen, 1 DMA transfers
the stream ends mid-recording (the emulator closed first); every whole frame was kept
this Mesen does not export 65 fields, among them cpu.a, cpu.x, cpu.y; their bits are zero
Mesen does not export $212A, $212B, $2130, so they read as zero until the game writes them
Format:       1.2
Producer:     Mesen recorder stream 3
ROM SHA-256:  a2462f1d74f993d0b3e10ae9425cd9b48253ac82c99913f07ef704d9c85dc6ee
Frames:       11 (1 keyframe, every 60 frames)
Storage:      5596 bytes, zstd
              keyframes 560 bytes each on average, deltas 371
Regions:      cpu 16, ppu 256, io 128, vram 65536, cgram 512, oam 544, timing 16
Layers:       write log, register writes by line
[exit code 1]
Error: packing <tmp>/clean.rlstream

Caused by:
    the stream was not recorded from this ROM: the stream was recorded from a 32768-byte ROM; this one is 65536 bytes
created <tmp>/P.romlens for "ROMLENS TEST"
referred to <tmp>/r.romrec: 11 frames from Mesen; the recording stays where it is
<tmp>/r.romrec  11 frames, Mesen  (present)
wrote <tmp>/r.romrec: 12 frames from Mesen, 1 DMA transfers
this Mesen does not export 65 fields, among them cpu.a, cpu.x, cpu.y; their bits are zero
Mesen does not export $212A, $212B, $2130, so they read as zero until the game writes them
<tmp>/r.romrec  11 frames, Mesen  (changed since it was attached)
no longer referring to <tmp>/r.romrec; the file was left alone
[exit code 1]
Error: the project does not refer to <tmp>/r.romrec
wrote <tmp>/mesen_recorder.lua
1. In Mesen, open Debug > Script Window and load the script.
2. In the script window's settings, allow access to I/O and OS functions, then run it.
3. Play, then stop the script and run: romlens rec pack <stream> --rom <rom> --out <recording>.romrec
   The stream goes to Mesen's script data folder unless ROMLENS_REC_OUT names a file.
