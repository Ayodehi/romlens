/* RESET_008000 at $00:8000: 10 instructions, clean level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

void SUB_008020(void); /* Reads nothing; returns X. */
void SUB_008030(void); /* Reads nothing; returns nothing. */
void SUB_008040(void); /* Reads nothing; returns nothing. */
void SUB_008050(void); /* Reads X; returns nothing; preserves X. */

/* Reads nothing; returns A, X and the carry (some callers are not known, so returns are assumed). */
void RESET_008000(void)
{
    SEI();
    native_mode();
    /* 8-bit A, X and Y */
    SUB_008020();
    SUB_008030();
    SUB_008040();
    SUB_008050();

CODE_008016:
    goto CODE_008016;
}
/* SUB_008020 at $00:8020: 5 instructions, clean level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0200[];

/* Reads nothing; returns X. */
void SUB_008020(void)
{
    X = 0x0F;

/* ▸ Clear memory: Clears $7E:0200 to $7E:020F: 16 bytes, a byte at a time, indexed by X. */
LOOP_008022:
    ADDR_7E0200[X] = 0;
    X = (u8)(X - 1);
    if ((s8)X >= 0) goto LOOP_008022;
    return;
}
/* SUB_008030 at $00:8030: 7 instructions, clean level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u16 ADDR_7E0010;
extern u16 ADDR_7E0012;
extern u16 ADDR_7E0014;

/* Reads nothing; returns nothing. */
void SUB_008030(void)
{
    /* 16-bit A, 8-bit X and Y */
    ADDR_7E0014 = ADDR_7E0010 + ADDR_7E0012;
    /* 8-bit A, X and Y */
    return;
}
/* SUB_008040 at $00:8040: 6 instructions, clean level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0020;
extern u8 ADDR_7E0021;
extern u8 ADDR_7E0022;

/* Reads nothing; returns nothing. */
void SUB_008040(void)
{
    A = ADDR_7E0020;
    if ((u8)A >= ADDR_7E0021) goto SKIP_008048;
    A = ADDR_7E0021;

SKIP_008048:
    ADDR_7E0022 = (u8)A;
    return;
}
/* SUB_008050 at $00:8050: 5 instructions, clean level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0300;
extern u8 DATA_008070[];

/* Reads X; returns nothing; preserves X. */
void SUB_008050(void)
{
    ADDR_7E0300 = DATA_008070[X];
    return;
}
