/* RESET_008000 at $00:8000: 10 instructions, full level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

u8 SUB_008020(void); /* Reads nothing; returns X. */
void SUB_008030(void); /* Reads nothing; returns nothing. */
void SUB_008040(void); /* Reads nothing; returns nothing. */
void SUB_008050(u8 x); /* Reads X; returns nothing; preserves X. */

/* Reads nothing; returns A, X and the carry (some callers are not known, so returns are assumed). */
u16 RESET_008000(u16 *x_out, bool *c_out)
{
    u8 x;

    SEI();
    native_mode();
    x = SUB_008020();
    SUB_008030();
    SUB_008040();
    SUB_008050(x);
    for (;;) {
    }
}
/* SUB_008020 at $00:8020: 5 instructions, full level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0200[];

/* Reads nothing; returns X. */
u8 SUB_008020(void)
{
    u8 x;

    for (x = 0x0F; (s8)x >= 0; x--) {
        ADDR_7E0200[x] = 0;
    }
    return x;
}
/* SUB_008030 at $00:8030: 7 instructions, full level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u16 ADDR_7E0010;
extern u16 ADDR_7E0012;
extern u16 ADDR_7E0014;

/* Reads nothing; returns nothing. */
void SUB_008030(void)
{
    ADDR_7E0014 = ADDR_7E0010 + ADDR_7E0012;
}
/* SUB_008040 at $00:8040: 6 instructions, full level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0020;
extern u8 ADDR_7E0021;
extern u8 ADDR_7E0022;

/* Reads nothing; returns nothing. */
void SUB_008040(void)
{
    u8 a;

    a = ADDR_7E0020;
    if (a < ADDR_7E0021) {
        a = ADDR_7E0021;
    }
    ADDR_7E0022 = a;
}
/* SUB_008050 at $00:8050: 5 instructions, full level. From Romlens; for reading, not for rebuilding the ROM. */
#include "snes.h"

extern u8 ADDR_7E0300;
extern u8 DATA_008070[];

/* Reads X; returns nothing; preserves X. */
void SUB_008050(u8 x)
{
    ADDR_7E0300 = DATA_008070[x];
}
